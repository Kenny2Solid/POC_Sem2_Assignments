grid = [
    ["-", "-", "-"]
    ["-", "-", "-"]
    ["-", "-", "-"]
]

def print_grid():
    for row in range(len(grid[row])):
        for col in range(len(grid[row])):
            print(grid[row[col]])
